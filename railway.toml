[build]
builder = "nixpacks"
# Force fresh install on Linux to resolve Rollup native binary issues
buildCommand = "rm -rf node_modules && rm -f package-lock.json && npm install && npm run build"

[deploy]
startCommand = "npm start --workspace=backend"
healthcheckPath = "/health"
healthcheckTimeout = 180
servicePort = 4000
