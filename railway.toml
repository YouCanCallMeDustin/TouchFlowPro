[build]
builder = "nixpacks"
# Use npm install which is smarter about platforms than npm ci
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm start --workspace=backend"
healthcheckPath = "/health"
healthcheckTimeout = 120
servicePort = 4000
