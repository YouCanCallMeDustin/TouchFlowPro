[build]
builder = "nixpacks"
nixpacksConfigPath = "nixpacks.toml"
buildCommand = "npm run build --workspace=frontend && npm run build --workspace=backend"

[deploy]
startCommand = "npm start --workspace=backend"
healthcheckPath = "/health"
healthcheckTimeout = 300
